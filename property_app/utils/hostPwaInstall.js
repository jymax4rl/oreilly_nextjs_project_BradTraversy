/** Entry URL: install-as-app first, then host application. */
export const BECOME_A_HOST_HREF = "/host/install?next=/host/onboarding";

export const HOST_INSTALL_SEEN_KEY = "isisel_host_pwa_install_seen";
